{
	"private": true,
	"scripts": {
		"dev": "nuxt",
		"build": "nuxt build",
		"preview": "nuxt preview",
		"generate": "nuxt generate",
		"vercel-build": "yarn global add node-gyp-build && yarn build"
	},
	"devDependencies": {
		"@nuxtjs/vercel-builder": "^0.24.0",
		"node-stdlib-browser": "^1.2.0",
		"nuxt": "3.0.0",
		"rollup-plugin-polyfill-node": "^0.11.0"
	},
	"dependencies": {
		"ethers": "^5.7.2",
		"vue-dapp": "^0.6.2"
	}
}
